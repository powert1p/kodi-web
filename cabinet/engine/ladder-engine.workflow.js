// Движок авторинга reasoning-лесенок: автор -> критик -> (<=1 правка) на задачу.
// Промпты в стиле Карпатого: суть + definition of done (проверяемые критерии), без worked-примеров
// и пошагового алгоритма. Читаемые копии — engine/author-prompt.md / engine/critic-prompt.md (синхрон!).
// args = { tasks: TaskRecord[] }
// TaskRecord = { node_id, topic, statement_ru, answer, primary_micro_skill, steps:[{n,instruction_ru,micro_skill,expected_value}], wrong_answer?, misconception? }
export const meta = {
  name: 'ladder-authoring-engine',
  description: 'Author reasoning-ladders from grounded tasks: author -> critic -> revise',
  phases: [
    { title: 'Author', detail: 'one author agent per task (grounded on steps[])' },
    { title: 'Critic', detail: 'rubric critic per ladder' },
    { title: 'Revise', detail: 'one revision pass if critic says revise' },
  ],
}

const AUTHOR = [
  'Ты собираешь «лесенку понимания» для кабинета работы над ошибками НИШ: ребёнок (10–17 лет, RU/KZ) написал срез, ошибся в задаче, и ты ведёшь его к ПОНИМАНИЮ этой задачи маленькими шагами-ступеньками.',
  '',
  '# Суть (зачем лесенка вообще существует)',
  'Беглый решатель проходит задачу, не замечая ОДНОЙ скрытой развилки — выбора, который он делает на автомате (от какой суммы взять процент? это одна величина или разные? на сколько частей делю? что с чем сравниваю?). Ребёнок споткнулся именно на ней. Лесенка — это разговор терпеливого репетитора, который подводит к этой развилке так, чтобы ребёнок САМ её увидел. Ты не пересказываешь решение и никогда не выдаёшь финальный ответ — ты вскрываешь то место, где ломается мысль. Всё, что НЕ про это место (рутинный счёт, который ребёнок с такой ошибкой и так умеет), в лесенку не входит.',
  '',
  '# Что тебе дают (grounding — опираешься, арифметику не переизобретаешь)',
  'Условие; верный ответ; полное проверенное пошаговое решение steps[] (числа бери отсюда); что ответил ребёнок и где сломался (если не дано — назови сам типичную ошибку этого навыка и неверное убеждение за ней). Шаги решения — это материал, а не сценарий лесенки: твоя работа не переписать их арифметику в вопросы (её можно списать), а найти развилку, которая за ними стоит.',
  '',
  'Сначала реши задачу про себя как эксперт и в свободных мыслях нащупай: где именно автоматический выбор, который беглый решатель не замечает, а этот ребёнок сделал неверно. Это ядро лесенки. В JSON свои размышления не выводи.',
  '',
  '# Definition of done — этому ОБЯЗАНА удовлетворять готовая лесенка',
  'Это не подсказки «как», а проверяемые свойства результата. Прогони каждое перед выдачей; не выполнено — переделай.',
  '- Одна мысль на ступень. Если ступень разнимается союзом «и» (что стало И от какой суммы) — это две ступени.',
  '- Вопрос не содержит и не подсказывает свой ответ. Тест: ребёнок, который понял, и ребёнок, который не понял, должны ответить РАЗНО. Если оба ответят одинаково — вопрос называет/предобосновывает ответ, это брак. «Почему» и арифметика живут только в reveal.',
  '- Лесенка целит в развилку, а не в счёт. Тест простоты: мысленно убери ступень — если путь к пониманию не рушится, ступень была филлером, выкинь её. Рутинная арифметика, которую ребёнок с ЭТОЙ ошибкой и так выполнит, ступенью быть не должна; дай такое число готовым в условии следующей ступени.',
  '- Каждая идея — ровно один раз, по всей лесенке. Тест: если две ступени научили бы одного и того же ребёнка одному и тому же — это одна ступень, даже когда меняются числа, действующее лицо или словесная обёртка. Не разворачивай один навык в серию почти одинаковых ступеней.',
  '- Дистрактор в выборе = реальная ошибка, которую тут делают (в идеале — что реально ответил/подумал этот ребёнок), не выдуманная и не абсурдная. Выбрать верное должно означать реально отличить ошибку. Опции короткие, без «потому что» внутри.',
  '- Конкретное прежде абстрактного. Буквенную запись (2k, x/3) вводи только после того, как идея понята на числах/долях.',
  '- Лесенка доводит. К последней ступени ребёнок может сам прийти к ответу задачи — лесенка не обрывается на середине сборки.',
  '',
  '# Свобода формы (требование, не пожелание)',
  'Канонической формы НЕТ — нет «правильного скелета». Число ступеней, их порядок и тип каждой выводятся из ЭТОЙ задачи и места слома, а не из шаблона. Тип ступени — по типу ошибки: ошибка в ЧИСЛЕ (просчёт в известной операции) -> compute (ребёнок вводит значение); ошибка в ВЫБОРЕ (не та база/правило/величина, что с чем) -> choose (2–3 коротких варианта, дистрактор = неверное убеждение). Маленький промах -> короткая честная лесенка; глубокая ошибка -> длиннее. Открывайся тем, с чего НАЧИНАЕТСЯ непонимание. Если твоя лесенка по форме подошла бы как есть и к другой задаче — ты пошёл по шаблону, переделай её вокруг развилки именно этой задачи.',
  '',
  '# Выход',
  'Верни ТОЛЬКО JSON: { "rungs": [ { "type":"compute|choose", "prompt_ru":"...", "options_ru":["...","..."] (только для choose, 2–3 коротких варианта), "answer":"...", "reveal_ru":"..." (коротко, 1–2 строки; арифметика и «почему» — здесь), "_kc":"<один микро-навык>", "_targets":"<одна фраза: единственная развилка этой ступени>" } ] }',
  'Поля фиксированы; КОЛИЧЕСТВО, ПОРЯДОК и ТИПЫ ступеней — свободны. Поле reasoning не выводи (думай в уме). Лишних полей (n, role и т.п.) не добавляй.',
].join('\n')

const CRITIC = [
  'Ты — строгий методический ревьюер «лесенок понимания». Тебе дают: условие задачи, эталонное проверенное решение steps[] (каждый шаг = один микро-навык, числа верны), что ответил ребёнок и где сломался, и ЛЕСЕНКУ автора (rungs[]). Твоя работа — проверить лесенку против definition of done и вынести вердикт. Ты НЕ переписываешь сам — называешь индекс ступени, цитату нарушающего места, нарушенное свойство и конкретную правку.',
  '',
  '# Сначала — план (отдельно от вердикта)',
  'В plan коротко: какая РЕАЛЬНАЯ скрытая развилка у этой задачи (из steps[] и ошибки ребёнка) и что прицельно проверишь.',
  '',
  '# Definition of done — лесенка готова, только если выполнено ВСЁ',
  'Проверяемые свойства результата (не рецепт). Для каждого нарушения выпиши {rung_index, criterion, quote, fix}. verdict=revise при нарушении ЛЮБОГО свойства.',
  '- Одна мысль. Ступень спрашивает ровно одно? «и» / две величины / два действия -> нарушение, разбить.',
  '- Не выдаёт ответ (нет leading). Вопрос называет, намекает или предобосновывает верную сторону? Тест: понявший и непонявший ответили бы одинаково? -> нарушение (кардинальный грех). Fix: открытая развилка, вывод/число в reveal.',
  '- Целит в развилку, а не в счёт (нет филлера). Тест простоты: ступень — рутинная арифметика, которую ребёнок с ЭТОЙ ошибкой и так выполнит, и её удаление НЕ рушит путь к пониманию? -> филлер, нарушение. Fix: убрать, дать число готовым в следующей.',
  '- Каждая идея ровно один раз по ВСЕЙ лесенке (cross-rung). Две (и более) ступени учат одного ребёнка одному навыку — пусть на разных числах, лицах, в разной обёртке слов? -> нарушение (дубль/долбёжка). Fix: слить, указать какие.',
  '- Дистрактор = реальная ошибка этого ребёнка (по его ответу/убеждению), не выдуманная/абсурдная; опции короткие, без «потому что». Иначе -> нарушение.',
  '- Тип по ошибке. Ошибка в числе как choose, или подлинная развилка как compute? -> нарушение, сменить тип.',
  '- Конкретное прежде абстрактного. Буквам предшествует ступень на числах/долях? Холодная нотация -> нарушение.',
  '- reveal держит арифметику и «почему». Число/шаг/вывод из reveal протёк в prompt или опции? -> нарушение, вынести в reveal.',
  '- Форма из задачи, не шаблон. Лесенка не подошла бы как есть к другой задаче; порядок/число/типы из развилки этой? Обобщённый скелет -> нарушение.',
  '- Доводит. К последней ступени ребёнок может сам дойти до ответа; лесенка не обрывается на середине сборки? Иначе -> нарушение.',
  '- Grounding. Числа/ответы ступеней согласованы со steps[]/expected_value? Расхождение -> нарушение (needs_human=true).',
  '',
  '# Вердикт',
  'JSON: { "plan":"...", "findings":[{rung_index, criterion, quote, fix}], "verdict":"accept|revise", "needs_human":bool, "confidence":"high|medium|low" }. verdict="revise" при нарушении любого свойства. needs_human=true при расхождении с grounding, confidence=low или неоднозначном. findings пуст -> accept. Без общих «в целом норм» — только конкретные находки с цитатой и правкой.',
].join('\n')

const LADDER_SCHEMA = {
  type: 'object', additionalProperties: false,
  properties: {
    rungs: {
      type: 'array',
      items: {
        type: 'object', additionalProperties: false,
        properties: {
          type: { type: 'string', enum: ['compute', 'choose'] },
          prompt_ru: { type: 'string' },
          options_ru: { type: 'array', items: { type: 'string' } },
          answer: { type: 'string' },
          reveal_ru: { type: 'string' },
          _kc: { type: 'string' },
          _targets: { type: 'string' },
        },
        required: ['type', 'prompt_ru', 'answer', 'reveal_ru', '_kc', '_targets'],
      },
    },
  },
  required: ['rungs'],
}

const CRITIC_SCHEMA = {
  type: 'object', additionalProperties: false,
  properties: {
    plan: { type: 'string' },
    findings: {
      type: 'array',
      items: {
        type: 'object', additionalProperties: false,
        properties: {
          rung_index: { type: 'number' }, criterion: { type: 'string' },
          quote: { type: 'string' }, fix: { type: 'string' },
        },
        required: ['rung_index', 'criterion', 'quote', 'fix'],
      },
    },
    verdict: { type: 'string', enum: ['accept', 'revise'] },
    needs_human: { type: 'boolean' },
    confidence: { type: 'string', enum: ['high', 'medium', 'low'] },
  },
  required: ['plan', 'findings', 'verdict', 'needs_human', 'confidence'],
}

const _a = typeof args === 'string' ? JSON.parse(args) : (args || {})
const tasks = _a.tasks || []
log(`engine: получено задач = ${tasks.length}`)

const taskBlock = (t) =>
  '=== ЗАДАЧА (grounding) ===\n' +
  JSON.stringify(
    {
      node_id: t.node_id, topic: t.topic, statement_ru: t.statement_ru,
      answer: t.answer, primary_micro_skill: t.primary_micro_skill, steps: t.steps,
      wrong_answer: t.wrong_answer ?? '(нет данных — назови самую частую ошибку навыка сам)',
      misconception: t.misconception ?? '(определи сам)',
    },
    null, 1,
  )

const results = await pipeline(
  tasks,
  (t) =>
    agent(AUTHOR + '\n\n' + taskBlock(t), {
      label: `author:${t.node_id}`, phase: 'Author', schema: LADDER_SCHEMA, effort: 'medium',
    }),
  (ladder, t) =>
    agent(CRITIC + '\n\n' + taskBlock(t) + '\n\n=== ЛЕСЕНКА ОТ АВТОРА ===\n' + JSON.stringify(ladder, null, 1), {
      label: `critic:${t.node_id}`, phase: 'Critic', schema: CRITIC_SCHEMA,
    }).then((critic) => ({ ladder, critic })),
  (cr, t) => {
    if (!cr || cr.critic.verdict === 'accept') {
      return { node_id: t.node_id, ladder: cr?.ladder ?? null, critic: cr?.critic ?? null, revised: false }
    }
    return agent(
      AUTHOR + '\n\n' + taskBlock(t) +
        '\n\n=== ТВОЯ ПРЕДЫДУЩАЯ ЛЕСЕНКА ===\n' + JSON.stringify(cr.ladder, null, 1) +
        '\n\n=== ЗАМЕЧАНИЯ КРИТИКА (исправь ТОЧЕЧНО) ===\n' + JSON.stringify(cr.critic.findings, null, 1),
      { label: `revise:${t.node_id}`, phase: 'Revise', schema: LADDER_SCHEMA, effort: 'medium' },
    ).then((l2) => ({ node_id: t.node_id, ladder: l2, critic: cr.critic, revised: true }))
  },
)

return { count: results.filter(Boolean).length, results }
