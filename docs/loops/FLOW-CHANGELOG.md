# FLOW-CHANGELOG — докрутки генератора (kodi-web)

> Докрутки генератора — ГЛАВНЫЙ продукт цикла benchmark-loop: артефакты сдаются и забываются, починки флоу компаундятся на все следующие run'ы. Раунд ниже планки → оркестратор ищет паттерн в дельтах судей → чинит генератор (промпт maker'а / рубрику / скилл / канон) → строка сюда ДО следующего раунда. Кросс-переносимые уроки дизайн-флоу — также в `~/Documents/design-lab/FLOW-CHANGELOG.md`.

**Формат строки:** `дата · run · что чинил во ФЛОУ · паттерн-причина (дельты каких судей)`

| Дата | Run | Что чинил во флоу | Паттерн-причина (дельты каких судей) |
|------|-----|-------------------|--------------------------------------|
| 2026-07-15 | webapp-v11 production preflight | **Role gate до jury:** длинные условия обязаны проходить content-role check (body face для чтения, display только для коротких marks/answer); один global progress + один causal local node; fake status/duration запрещены detector-тестом. State harness теперь strict API и сначала проверяет auth + 44 production states, normal/reduced motion, focus и input preservation | Независимый production-0 audit + own-eyes: Tektur превратил условия в HUD; Drill/Srez дублировали progress; six-screen happy-path скрывал focus/error/network и fake Hub copy |
| 2026-07-15 | codex-design-flow-adaptation | **Два режима + generator-level loop:** known-shape UI получает конечный maker→gates→blind review pipeline; rejection/clean slate/«до планки» — frozen generator-loop. Ниже creative bar budget/plateau дают только `NOT_READY`; текущий Codex = единственный maker/integrator, native agents = blind judges. Удалены stale inputs: unconditional DS canon и extreme/BOLD prompt | Решение владельца в сегодняшнем Claude-чате + consensus Round 1: прежний loop докручивал output и разрешал early satisficing; роли и terminal judge gate расходились, а global `frontend-design`/fallback rubric снова тянули output к rejected стилю |
| 2026-07-13 | webapp-v6 R1 | **Канон-гравитация:** webapp/DESIGN_SYSTEM.md получил v6-баннер («не строй по v5»), агент-память ui-designer переписана на v6 ДО имплементационного раунда | Фидбек владельца: в другом проекте агенты при новом дизайн-флоу воспроизводили старый вид, т.к. читали устаревший канон-файл/память («tokens are LAW» v4/v5) |
| 2026-07-13 | webapp-v6 R1 | Шрифт-верификация стала МЕХАНИКОЙ оркестратора (probe + spread-тест в гейте R2), self-report maker'а не принимается | J1 приписал верификацию не тому варианту, K-A сдал Days One без кириллицы при «пройденной» самопроверке — self-report ненадёжен (J1/J2/J3 разошлись в фактах) |
| 2026-07-13 | webapp-v6 R1 | В брифы UI добавлено: плейсхолдер/inputmode формат-нейтральны и никогда не содержат ответов; instruction из данных UI не дублирует | J3: K-B placeholder «Например, 40 тенге» = финальный ответ (craft-потолок 4); кросс-паттерн J3 про ступень-1 (ответ в instruction — дефект данных банка, не UI) |
| 2026-07-13 | webapp-v6 R1 | SYNTHESIS-директивы №1-3 (число-герой НА маршруте в первом вьюпорте; кривая рисуется dashoffset; активная ступень перевешивает середину drill) вшиты в R2-промпт | Кросс-паттерны: J2 «signature не командует первым кадром hub у всех трёх», J1 «середина drill — стек равного веса у всех трёх», J2/J3 «линия K-B = ruler-straight таймлайн, не маркер» |
| 2026-07-13 | webapp-v6 R2 | **«Нулевая секунда» для signature:** приём обязан читаться в СТАТИКЕ (след-подложка + плотный штрих), draw-анимация — усиление, не условие видимости; вшито в DELTAS/R3-промпт | J1 «маршрут тихий, пунктир 1 11 почти невидим», J2 «в рендере нет линии — draw прячет до 1.35с» — анимационно-зависимая сигнатура умирает на скриншотах и у ребёнка с reduced-motion |
| 2026-07-13 | webapp-v6 R2 | Локаль-инвариант в канон моков: валюта/единицы = прод-банк (₸/тенге), сверка между экранами; + AA-чек токенов (muted/disabled/граф.элементы ≥3:1) на этапе ТОКЕНОВ, не пост-фактум | J1+J2+J3 «₽ против ₸ в одном флоу»; J3 «--muted 3.43:1, disabled CTA 2.98:1 — блокирует СУПЕР» |
| 2026-07-13 | webapp-v6 R2 | render-app.mjs: login в отдельном анонимном контексте (jwt-инжект редиректил на hub → слепая зона панели); кульминационные состояния (closure celebrate) — dev-достижимы для капчера | Процедурные флаги J1/J2/J3: login.png = дубль hub; celebrate не виден панели («судил по коду») |
