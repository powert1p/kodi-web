# Concept gate result

## Decision

**Selected macro-direction: A — «Лента решения».** Два из трёх независимых reviewers выбрали A и поставили `wow: true`; specialist dissent выбрал C из-за более мягкой атмосферы и жёстче трактовал source transformation.

## Why A

- Лучше всего сохраняет math-first на Hub, Drill и Srez.
- Cream/forest/orange развивает исходный AiPlus без orange wall и без взрослого luxury/editorial тона.
- Vertical proof tape даёт собственную пространственную и interaction signature.
- C ближе к generic floating-card/orbit AI UI; B возвращает официальный worksheet/workbench язык.

## Consensus deltas frozen for production

1. Уменьшить Hub headline и поднять math/action в первые две секунды.
2. Tektur использовать только для короткого display/math; инструкции и microcopy — Onest.
3. Белке дать отдельный контекстный слот без пересечения текста; не использовать PNG со старым logo на одежде; active Drill/Srez остаются mascot-free.
4. Header использует самостоятельный code-native `ai+` seed mark без exact lockup.
5. Proof rail строится из реальных steps: orange active marker → green solved marker → открывается следующий узел; reduced-motion показывает мгновенную эквивалентную смену.
6. Input должен иметь явный affordance, focus, error, preserved-value network state.
7. Production evidence обязано включить loading/error/empty/success и reduced-motion — concept renders этого не доказывают.

Concept gate: **PASS**. Production READY: **NOT_YET**.
