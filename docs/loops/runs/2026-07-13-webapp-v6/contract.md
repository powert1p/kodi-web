# contract — редизайн мобилки webapp/ по дизайн-системе v6 (run 2026-07-13)

**Бриф:** владелец построил новый дизайн-флоу (design-lab FLOW v3: SHAPE → генерённые ассеты gpt-image-2 → BUILD → self-render → панель) и поставил задачу: «переделай по ней дизайн мобилки… очень красивый дизайн топового уровня с охрененным UX/UI». Дизайн-направление v6 «Тетрадь чемпиона» — `DESIGN-V6.md` этого run'а (регистр, палитра, тип-пара, signature «маршрут маркером по клетке», генерённые иллюстрации).

**Рубрика (заморожена до конца run'а):** `docs/loops/rubric-design.md` (оверрайд) поверх канона `~/Documents/design-lab/AUDIT-RUBRIC.md`. Планка = **СУПЕР** канона: все 8 осей ≥8 у каждого судьи · avg ≥8.5 · панч ≥8 · detect 0 критичных · тех-гейт (console/скролл/AA/focus) · wow ≥2/3.

**Структура раундов:**
- **R1 — концепты:** 3 maker'а (ui-designer) × 3 экрана (hub/drill/srez) статичным HTML 390px с ассетами → механика (detect + мой рендер/глаза) → панель 3 судьи × 3 варианта → победитель + паттерн дельт.
- **R2 — порт в React:** победивший концепт → tokens.css v6 + Ap*-компоненты + ВСЕ экраны приложения (login/hub/srez/drill/analytics/closure); гейты кода (vitest · lint:design перекалибровать · build · vite-рендер) → панель по живым экранам.
- **R3(-R4) — дельты:** консенсус-дельты панели → фиксы генератора/кода → финальная панель.

**Бюджеты:** 4 раунда ИЛИ ~3 часа wall-clock (планка «супер» затребована владельцем явно). Выбит бюджет или thrash 2 раунда → СТОП + эскалация тремя строками.

**DoD run'а (сверх планки):** vitest/lint:design/build зелёные · design-baselines пересняты · webapp/DESIGN_SYSTEM.md обновлён до v6 (канон = итог, не намерение) · vite-рендер всех экранов чистый. Деплой и merge — ПОСЛЕ планки, из главного цикла (не изнутри).

**Число maker-вариантов:** R1 = 3 (углы K-A/K-B/K-C из DESIGN-V6), далее 1 maker на раунд.
**Механика (гоняет оркестратор):** detect.mjs · render-mobile.mjs (390×844 full-page) · в R2+ вдобавок loop-verify-слой кода: `npx vitest run` + `npm run lint:design` + `npm run build`.
**Судьи:** 3 независимых opus-агента, линзы: (1) типографика+композиция, (2) характер/панч/signature (адвокат ребёнка 10-13), (3) craft/UX-состояния/честность данных. Вердикт строго JSON рубрики.
