# R3 → R4: финальные микро-дельты (панель: J1 7.88 · J2 8.13 wow · J3 8.63 SUPER wow → avg 8.21, порог 8.5)

Последний раунд бюджета. ТОЛЬКО пункты ниже, ничего сверх. Порядок = приоритет.

## 1. LOGIN — первый экран ребёнка, консенсус J1+J2 (главная причина punch 7)
`LoginPage.tsx:288` — RouteMeter живёт только на шагах регистрации (`regIdx>=0`). Сделать: маршрут виден и на ВХОДНОМ шаге (телефон) — как «начало пути» (первая отметка активна, остальные впереди); Кёди крупнее (m вместо s, если сейчас s); нижняя пустота — заполнить тёплым слоем (клетка+иллюстрация-полоска или растянуть композицию по dvh). Один primary-CTA сохраняется.

## 2. CLOSURE (рабочее состояние, pre-submit) — J3+J1: ~35% мёртвой клетки внизу
Дать тот же якорь, что срезу: иллюстрация-полоса (провизия `src/assets/camp-start.jpg` уже в репо) ЛИБО высотная центровка формы (min-h-[Ndvh] паттерн) — низ не должен быть пустой третью экрана.

## 3. SREZ — J1: гигант «1/12» ломает реплику Кёди в рваную 5-строчную колонку
Развести: числу — свой ряд/блок (не бок-о-бок с текстом), реплика Кёди полной шириной. Проверить рендером 390.

## 4. KaTeX-сериф (J1, системный шов) — АККУРАТНО, с визуальным гейтом
Числа задач рендерятся Computer Modern serif внутри sans-приложения. Попробовать override в index.css: `.katex { font-family: 'Golos Text', sans-serif; }` (+ `.katex .mathnormal` при необходимости) — наша математика простая (числа/проценты/дроби). ОБЯЗАТЕЛЬНО отрендерить drill/srez/hub/closure после: дроби/степени не должны разъехаться. Разъезжаются → откатить override и записать в отчёт «KaTeX оставлен CM: ломает layout» (честный откат лучше кривой математики).

## 5. Копейки регистра (J2/J3):
- «+30 XP» → «+30 очков» (closure celebrate).
- hub: рассинхрон «4 ошибки ждут» vs «1/5 · 20%» — привести к одной системе прочтения: hero «4 из 5 ждут разбора», прогресс «Разобрана 1 из 5 · 20%» (числа честные из тех же данных).
- disabled «Проверить»: вместо серого — brand-soft заливка + brand-ink текст (честно-неактивна, но не «мёртвая»); контраст ≥4.5:1 сохранить.

## 6. Мелочь (J3): trace-подложку маршрута затемнить до ≥3:1 (`--route-trace`), analytics «последний раз»/«×N» 11px → 13px label.

## Гейты (все, сам): tsc · vitest 52 · lint:design · build · рендер всех 7 экранов (render-app.mjs, vite на 5199 уже может бежать — проверь/перезапусти) → СМОТРИ глазами KaTeX-математику и login. Скрины → rounds/round-4/self/. Тесты/рубрику не править. push не делать.
