# Вердикт: аудит тематического слоя графа v02 → патч v03

**Дата:** 2026-07-06 · **Метод:** Workflow 60 агентов (8 линз sonnet: 6 strand-зон + цикл + coverage; adversarial verify каждой находки на opus) → судейство Fable по спорным. 52 находки → 22 подтверждено → **9 изменений данных принято**, остальное — КТП-заметки и отклонения.
**Факты для аудита:** `.superpowers/graph-audit/graph-facts.md` (узлы-DAG 114/114 подтверждён Kahn; тематический слой: 1 цикл из 4 тем, 91 межтемная пара, 10 стоков).

## Принятые изменения графа (патч v03)

**Дроп рёбер (5+1) — правило «пререквизит только если задачи нерешаемы»:**
1. `AR09→CV04`, `AR09→CV05` — конвертация км²/м³ не требует узла «Степени»; содержательный вход остаётся (GE02→CV04→CV05). Убирает обратное 6.EE.A→5.MD.A.
2. `GE01→GE12` — «периметр» не нужен для подсчёта фигур на чертеже; GE12 сохраняет CB01.
3. `EQ02→WP03` — движение по воде решается арифметикой (v±течение); WP03 сохраняет WP02. Убирает обратное 7.EE.B→6.RP.A. (3 линзы независимо; verify оспорил только риторику «ни одна», не необходимость.)
4. `DV02→LG04` — ребусы «AB+BA=99» не требуют делителей.
5. `AL01→EQ02` — транзитивная редукция после добавления AL02→EQ02 (AL01→AL02→EQ02).

**Добавление рёбер (3):**
1. `AL02→EQ02` — 65%+ задач «Линейные уравнения» требуют раскрытия скобок; AL01 (подстановка в выражение) этому НЕ учит — опровержение отклонено по фактическим задачам AL01. Чинит и цепочку головы EQ03.
2. `DC01→AR07` — AR07 содержит «Округлите 6.478 до десятых»: нерешаемо без понятия десятичной дроби. AR06→AR07 остаётся (целочисленное округление).
3. `AR10→LG04` — ребусы требуют разрядной записи (AB = 10A+B) — это ровно AR10.

**Переименования тем (3, view-слой):**
- `4.NF.B` → «Смешанные числа и неправильные дроби» (числовой прямой в узлах нет).
- `NIS.ALG` — в имени «квадратные» → «неравенства» (3 из 6 узлов — неравенства; квадратных уравнений в скоупе нет).
- `4.MD.A` — убрать «перевод единиц» из имени (перевода там нет; не называть «Площадь фигур» — конфликт с 6.G.A; взять «Площадь прямоугольника и квадрата» по фактическому составу).

**Переносы задач (5, с текст-гардами перед UPDATE):**
- LG06: id 775 (сумма-разность) → EQ07; id 772 (простое сложение) → AR01; id 777 (рукопожатия C(5,2)) → узел сочетаний NIS.COMB (CB01/CB05 — выбрать по name_ru узла).
- WP08: задача «Айдар/Болат/Досан… забор» (совместная работа) → WP05.
- DA01: задача «Сколько мод…» (без диаграммы) → DA03.

## Отклонено (моё судейство, вопреки части линз)

- **Перепривязки AR01→3.OA.B и AR08→5.OA.A** — контент-аргументы против сильны (AR01 реально содержит многозначную арифметику → дом 4.NBT.B верен; 3.OA.B «Свойства умножения» — канонический CC-дом для «выгодного способа»). Цикл тем {3.OA.A, 3.OA.B, 4.NBT.B, 5.OA.A} НЕ дефект пререквизитов (узловой граф — DAG), для КТП ведём эти темы одним блоком «Арифметика целых».
- **28 опровергнутых находок** verify-стадии (в т.ч. «нет узла объёма» — есть; «нет неравенств» — есть в банке; перенос кусков DA02/WP10 — посылки ложны) — без действий.

## Следствия для целостности

- Рёбер узлов: 181 → **178** (−5−1+3). Узловой DAG сохраняется (нет обратных путей у добавленных: AR07 и LG04 — стоки, EQ02 без пути в AL02 — проверено).
- Гейт: `test_graph_semantics` (обновить ожидание счётчика рёбер), пересборка топо-порядка, JSON+dev+prod синхронно, бэкапы перед UPDATE.
- КТП-заметки (порядок 5.NF.A→5.NF.B, внутренний порядок 6.RP.A, GE08 после PR02+CV02, блок «Арифметика целых», факультативность 3.MD.D/4.G.A/6.SP.B для экзамен-трека, методический порядок зоны SP/LOGIC/NT) — уходят в `docs/plan/ktp-v1.md`, не в граф.
