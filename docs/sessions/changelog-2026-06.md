# Сессии — 2026-06

## 2026-06-22 15:47
**Тип:** plan (revival, 3 фазы)
**Зачем:** воскресить старый learning-эпохи проект kodi-web (адаптивная платформа по математике НИШ, последний коммит 2026-03-02): аудит → каркас доков → деплой на свой сервер, чтобы снова работал и можно было развивать.
**Что сделано:**
- Ф1 аудит (Workflow 5 ридеров + adversarial): AUDIT-REPORT.md. Вердикт — жив/воскрешаем; 1 P0 (untracked шрифт ломал build), 4 P1, долг ≈0 тестов.
- Ф2 каркас по образцу CDP: PROJECT.md, slim CLAUDE.md, DESIGN_SYSTEM.md, docs/{architecture,module-map,data-state}.md, .claude/rules/*, ADR-001. Исправлены неточности старого CLAUDE.md (2525 задач/118 нод/8 таблиц).
- Ф3 фиксы P0/P1 + docker-compose (свой Postgres) → локальный Docker → smoke ядра → деплой на VPS.
**Решение:** деплой self-hosted на тот же VPS, что CDP (aiplus), отдельный compose-дир ~/kodi-web, свежая БД, порты 8300/5435 (не Railway — выбор владельца; ADR-001). Стек kodi-web (SQLAlchemy async/asyncpg, Flutter) сохранён, не переписан под CDP.
**Итог:** ✅ задеплоено и живо на проде — kodi-app/kodi-postgres healthy, /health 200, core-флоу (register→задача→ответ→BKT p_mastery 0.305) зелёный, seed 118/2525, 0 ошибок. Авто-ревью (reviewer) APPROVE. Доступ с мака — SSH-туннель (ssh -L 8300:127.0.0.1:8300 aiplus). 5 коммитов в origin/main.
**Ключевая находка:** smoke-тест поймал P0, которого статический аудит не видел — timestamp-колонки naive vs datetime.now(timezone.utc) aware → asyncpg DataError на /practice/next (весь цикл практики падал). Фикс timezone=True заодно починил 2 латентных краша в spaced-repetition (selector.py:149,380).
**Открытые вопросы:** nginx vhost + SSL = root/Умид (домен подтвердить) — пока туннель; backlog аудита (тесты, Alembic-миграции, разбить routes.py 1070 строк, заменить Roboto-шрифт, dead code scorers/classifiers).
**Файлы:** AUDIT-REPORT.md, docker-compose.yml, backend/db/models.py, backend/{run,api/routes,core/config,core/grading}.py, .claude/rules/deploy.md
**Issue:** —

## 2026-06-23 11:27
**Тип:** plan (редизайн обучения, продолжение)
**Зачем:** решить «что брать — граф или скиллы/темы» для редизайна kodi. Владелец: пусть граф Common Core = общий стандарт тем, микро-навыки поставить под темы, убрать дубли, слить «про одно и то же».
**Что сделано:**
- Реструктуризация таксономии **372→337 микро-навыков** под граф Common Core (Workflow: 15 агентов чистят домен + мапят на CC-стандарт → консолидация кросс-дублей → 40 скептиков проверяют спорные маппинги).
- Итог: 3 уровня — **РАЗДЕЛ CC (домен) → ТЕМА (стандарт-кластер, узел графа со стрелками) → микро-навык**. 43 темы (39 CC-кластеров + 4 НИШ-темы: логика/комбинаторика/теория чисел/продвинутая алгебра), 61 готовое ребро-пререквизит из CC Coherence Map.
- Дедуп: 12 кросс-доменных дублей + 9 по ревью; 5 навыков НИШ→CC по якорю. Reviewer-агент нашёл 5 дефектов (врущий strand, мёртвые разделы, остаточные дубли) — исправлены в этом же заходе.
**Решение:** граф = Common Core (готовый, не изобретаем); strand = домен CC (а не старые 15 «доменов» — те давали кросс-дубли и пустые разделы). Слияние консервативное (каталог уже 902→372). Покрытие гарантируется инвариантом в Python, не доверием к агентам.
**Итог:** ✅ артефакты на диске, инвариант держится — **372/372 исходных кода учтены, 0 потеряно, Σ частот 7036→7037**. Все топ-навыки (дроби/проценты/уравнения) сели в верный CC-стандарт (спот-чек).
**Открытые вопросы:** 6 навыков  на ревью грейна (int_add_sub сел на 7.NS; RP-раздел крупный 61 навык/2 темы); сшивка КТП-уроков с этой структурой — отдельный заход; решить, строить ли тренажёр поверх.
**Файлы:** docs/specs/cc_topic_skill_tree.{json,md}, docs/specs/2026-06-23-cc-graph-topics-microskills-restructure.md
**Issue:** —

## 2026-06-23 16:00
**Тип:** plan (новая фича + движок)
**Зачем:** «работа над ошибками» для НИШ-срезов — кабинет, где ребёнок разбирает заваленную задачу по подзадачам, ведущим к ПОНИМАНИЮ (не выдавая ответ); при затыке — задача попроще; рядом AI-тьютор. Старый Flutter-флоу владельцу не нравится.
**Что сделано:**
- **Новый фронт-кабинет** `cabinet/` (React 19 + TS + Vite SPA + Tailwind v4 + KaTeX + Zustand, HashRouter) — отдельно от Flutter, embeddable в мобилку через WebView позже. 5 экранов: Срез-хаб → Лесенка разбора (signature: ступени-«рунги», gating) → AI-тьютор (bottom-sheet, mock) → Закрытие. Mock-данные по форме backend-декомпозиции (тема→микро-навык→задача-в-шагах).
- **Reasoning-first лесенки:** ступень = ОДНА мысль; типы `compute` (ввод) и `choose` (выбор-развилка под ловушку); `reveal` (арифметика) — только при затыке; climb-down «задача попроще» при 2 ошибках.
- **Реальные НИШ-задачи** из базы (`source != generated`): проценты/множества/отношения/работа/движение — каждая со своей ловушкой.
- **Движок авторинга** `cabinet/engine/`: агент-АВТОР (промпт на принципах + контрастные ok/не-ok разной формы, БЕЗ скелета) → агент-КРИТИК (поштучный чеклист C1–C12, вердикт) → ≤1 правка → я. Grounding на эталонных `steps[]` (SymPy-проверены) — LLM делает только педагогику.
**Решение:** (1) НЕ Flutter Web — статичный Vite SPA легче в WebView. (2) Лесенка ≠ шаги решения: рунг вскрывает СКРЫТУЮ развилку (expert blind spot), а не переписывает видимую арифметику. (3) Анти-шаблон через ОТСУТСТВИЕ названного скелета в промпте + few-shot разной формы (называешь форму → модель её клонирует). (4) Движок — батч-офлайн (как декомпозиция), рантайм только читает.
**Итог:** ✅ кабинет билдится (tsc strict) + прогнан в Playwright (мобайл, 0 console-ошибок); движок прогнан на 3 задачах за **97 сек** — критик САМ поймал leading-вопрос на свежей задаче и автор переписал. Поймал и починил баг движка: жирное поле `reasoning` первым в structured-output → обрезка по токенам → «rungs missing» retry-loop (20–46 вызовов, 13 мин/задача). Фикс: reasoning не выводить, думать в reasoning-токенах.
**Открытые вопросы:** движок не прогнан на весь банк (2525) — overnight-батч позже; рантайм-эндпоинт + таблица `rungs` (контракт под mock) — отдельно; реальный AI-тьютор (SSE) вместо mock; снос/cutover Flutter — по явному согласию; реальные `wrong_answer` детей для дистракторов (пока эвристика).
**Файлы:** cabinet/ (React app), cabinet/engine/{author-prompt.md,critic-prompt.md,ladder-engine.workflow.js}, docs/specs/2026-06-23-{mistakes-cabinet-mvp,ladder-authoring-engine}.md
**Issue:** —

## 2026-06-24 14:34
**Тип:** ad-hoc (контент + движок)
**Зачем:** довести качество всех 7 лесенок среза до единого стандарта новых «карпатовских» промптов движка — старые a/c/e/f были ручные, g/h/i движковые; владелец: «распили все 7 по ней».
**Что сделано:**
- Прогнал все 7 задач (a/c/e/f/g/h/i) через `cabinet/engine` (Workflow: автор→критик→правка). Критик завернул **все 7** черновиков на `revise` — снял leading (вопрос подсказывал ответ), вынес арифметику из вопроса в `reveal`, выкинул филлер-ступени и дубли. Структурно ужал: f 3→2, g 5→3, i 7→5; каждая лесенка открывается развилкой, а не счётом.
- DV01 (i) упал на первом прогоне — критик 5× не уложился в StructuredOutput-схему на 5-ступенчатой лесенке (длинный вывод → невалидный JSON → ретраи кончились, pipeline[6]→null). Перезапустил DV01 изолированно (1 задача) → прошёл. К i применил точечный fix самого критика (убрал протёкший «+1» из reveal 3-й ступени) вместо второго прогона.
- Вписал все 7 в `cabinet/src/mock/srez.ts`: маппинг `rung→Step` (`_kc`→`microSkill`, `options_ru`→`options[{id,label}]`, `expected`=id/число), математика обёрнута в `$...$` KaTeX. Сплайс питоном по маркерам (не зависеть от точного совпадения старого блока). Diff: 1 файл, +134/−103.
**Решение:** интегрировать всё через движок (а не точечно править) — единый стандарт; ручную правку i предпочёл второму agent-проходу (критик уже выдал точный fix — дешевле и точнее). Сплайс питоном по start/end-маркерам вместо Edit всего блока — устойчивее к whitespace.
**Итог:** ✅ закоммичено `59e698c`. Гейты: `tsc + vite build` зелёный; Playwright — лесенка g сквозным проходом (choose→choose→compute→**closure** с проверочной задачей), h — KaTeX в опциях (`3x`, `x/3`) и уравнении (`19x/3=133`), i — 5-ступенчатая fencepost-лесенка; консоль браузера 0 ошибок.
**Открытые вопросы:** f (PC04) ужалась до 2 ступеней — продуктово ок, но без «разогрева» (можно вернуть вводную); i (DV01) — одна правка не слила ступени 3–4 максимально плотно (кандидат на 2-й проход критика); движок не прогнан на весь банк (overnight-батч позже).
**Файлы:** cabinet/src/mock/srez.ts, cabinet/engine/ladder-engine.workflow.js
**Issue:** —
